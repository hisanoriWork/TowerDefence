using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class negiTest : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //oyo
    }

    // Update is called once per frame
    void Update()
    {
        //check conflict by negi
    }
}
